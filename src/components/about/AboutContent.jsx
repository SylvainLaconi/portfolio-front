import React from 'react';
import { ContentContainer } from '../../styles/_containers';

const AboutContent = () => {
  return <ContentContainer>Coucou</ContentContainer>;
};

export default AboutContent;
